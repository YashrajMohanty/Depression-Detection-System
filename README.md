# Depression-Detection-System
A python based application that uses machine learning and deep learning models to predict if a person is susceptible to depression using symptoms as input.  
Uses a variety of machine learning models like KNN, logistic regression, SVM, decision tree as well as a feed forward DNN.
